export interface Book {
  title: string,
  subtitle?: string,
  author: string, 
  year: number,
  category?: string
  language: string, 
  country: string, 
  pages: number, 
  price?: number,
  link?: string, 
  status?: string,
  isbn?: string,
  description?: string,
  url?: string
}